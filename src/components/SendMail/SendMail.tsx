import { FC } from 'react'
import './SendMail.css'

interface ISendMailProps {}

const SendMail: FC<ISendMailProps> = (props) => {
  return <div className="send-mail"></div>
}

export default SendMail
